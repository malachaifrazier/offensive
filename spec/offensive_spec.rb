# frozen_string_literal: true

RSpec.describe Offensive do
  it "has a version number" do
    expect(Offensive::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
